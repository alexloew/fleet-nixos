{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:best-practices"
  ],
  "nix": {
    "enabled": true
  },
  "lockFileMaintenance": {
    "enabled": true,
    "automerge": true
  },
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/pkgs/default.nix/"
      ],
      "matchStrings": [
        "version = \"(?<currentValue>.*?)\"; # renovate: orbit\\s"
      ],
      "depNameTemplate": "fleetdm/fleet",
      "datasourceTemplate": "github-tags",
      "versioningTemplate": "semver",
      "extractVersionTemplate": "^orbit-v(?<version>.*)$"
    }
  ],
  "packageRules": [
    {
      "matchManagers": [
        "nix",
        "github-actions",
        "custom.regex"
      ],
      "automerge": true,
      "labels": [
        "dependencies"
      ]
    },
    {
      "matchPackageNames": [
        "fleetdm/fleet"
      ],
      "prBodyNotes": [
        "⚠️ **Note:** Renovate can only update the version string. Please run `./update.sh` locally to update hashes and metadata, or wait for the daily automated update workflow."
      ]
    }
  ]
}
